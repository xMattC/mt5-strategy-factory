// Auto-generated strategy

{{ input_lines | join('\n') }}

int indi_handle;

int OnInit() {
    indi_handle = iCustom(_Symbol, PERIOD_CURRENT, "{{ indicator_path }}");
    return INIT_SUCCEEDED;
}

void OnDeinit(const int reason) {}

void OnTick() {
    {% for buf in buffers %}
    double {{ buf.name }}[];
    ArraySetAsSeries({{ buf.name }}, true);
    CopyBuffer(indi_handle, {{ buf.index }}, 0, 10, {{ buf.name }});
    {% endfor %}

    bool long_in = {{ base_conditions.long }};
    bool short_in = {{ base_conditions.short }};

    // Manage orders based on long_in, short_in
    // --- Add your order management logic here ---
}
